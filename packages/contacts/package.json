{
  "name": "@ai-assistant/contacts",
  "version": "1.0.0",
  "description": "Contact resolution across channels (Phase 5)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "pnpm --filter @ai-assistant/database db:generate",
    "build": "node ../../node_modules/typescript/bin/tsc"
  },
  "dependencies": {
    "@ai-assistant/database": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~6.0.3"
  }
}
