# Reflection engine (planned)

**Not deployed.** Post-turn reflection and learning loops are not wired to this HTTP stub.

Cognitive orchestration lives in `services/cognitive-runtime` (mounted by `services/ai-runtime`).
