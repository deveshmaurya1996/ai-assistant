# Cognitive runtime reads the monorepo root env file (via cognitive_env_loader.py).
# In production it runs inside the intelligence process (same container as ai-runtime).
# See ../../.env.example for the full variable set.
