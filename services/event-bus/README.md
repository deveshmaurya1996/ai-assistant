# Event bus (planned)

**Not deployed** in the platform monolith. Gateway and workers use `@ai-assistant/events` in-process today.

This folder is scaffolding for a future standalone event observability service (Redis Streams / NATS). Do not add to Tilt or Render until wired.
