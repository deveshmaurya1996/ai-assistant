# Browser runtime (planned — Phase 9)

**Not deployed.** Playwright snapshot/execute is stubbed in `src/index.ts`.

Production automations use gateway workers and tool-runtime today. Revisit when browser agents need a dedicated process.
