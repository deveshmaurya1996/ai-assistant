# gateway reads the monorepo root env file:
#   Local:       ../../.env
#   Production:  ../../.env.production (NODE_ENV=production)
# On Render (ai-assistant-gateway): paste env from ../../.env.production
