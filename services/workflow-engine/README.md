# Workflow engine (planned)

**Not deployed.** Durable Temporal workflows are not wired yet.

Gateway runs BullMQ `workflow.worker.ts` for automations today. This package is a future Temporal worker scaffold.
